import { AppInit } from '../../interfaces/app-init';
declare const Routes: ({ templates, components, routes, headerComponent, footerComponent, pencilBannerComponent, loader }: AppInit) => JSX.Element;
export { Routes };
